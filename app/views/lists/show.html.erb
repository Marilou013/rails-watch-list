<h1><%= @list.name %></h1>
<% @bookmarks.each do |bookmark| %>
  <%= bookmark.comment %><br>
<% end %>
<%= link_to "Back to all lists" , lists_path %> <br>
<%= link_to 'delete', list_bookmark_path(@list),
          data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
